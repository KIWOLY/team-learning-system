# Frontend (React + Vite)

This folder contains a minimal React frontend scaffold for the Team Learning System.

Quick start

```bash
cd Frontend
npm install
npm run dev
```

This uses Vite + React. The app includes basic pages and components under `src/`.
